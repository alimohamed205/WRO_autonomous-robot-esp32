for ultrasonic sensor 
NewPing library for arduino
https://wiki.dfrobot.com/URM09_Ultrasonic_Sensor_(Gravity_Analog)_SKU_SEN0307





تمام، شكراً إنك وضحت إن مستشعر الـ Ultrasonic عندك هو Gravity: URM09 Ultrasonic Distance Sensor (Analog)! 😊 ده مستشعر مختلف عن الـ HC-SR04 لأنه بيطلع إشارة Analog بدل الـ Trigger/Echo الرقمي، وده بيخليه أسهل في الاستخدام مع الـ ESP32، بس محتاج طريقة مختلفة للقراءة. الـ URM09 بيقيس المسافات من 2 سم إلى 500 سم ويطلع الإشارة كجهد تناظري (Analog Voltage) يتم تحويله لمسافة.

هنختبر الـ URM09 خطوة بخطوة على الـ ESP32، وبعدين لو عايز ندمج الأربعة مستشعرات أو ننتقل لمكون تاني (زي الـ Servo أو الـ ESC)، قولي.
الخطوة الأولى: اختبار مستشعر URM09 Ultrasonic (واحد من الأربعة)
مواصفات الـ URM09 (Analog):

    نطاق القياس: 2 سم إلى 500 سم.
    الإخراج: إشارة Analog (الجهد بيتراوح من 0V إلى 3.3V أو 5V حسب مصدر الطاقة).
    الاتصال:
        VCC: بيشتغل على 3.3V أو 5V (اختار 3.3V مع الـ ESP32 للأمان).
        GND: وصل بـ GND.
        OUT: الإشارة التناظرية (Analog Output) لازم تتصل بـ Analog Pin على الـ ESP32 (مثل A0 أو Pin 34).
    معادلة تحويل الجهد إلى مسافة: حسب الوثائق الرسمية لـ DFRobot، الجهد الخارج يتحول لمسافة بالعلاقة:
        المسافة (سم) = (قراءة Analog * 500) / 1023 (لو 10-bit ADC).
        لو بتستخدم 3.3V، القراءة هتكون نسبية للجهد ده.

الاتصال:

    VCC: وصل بـ 3.3V على الـ ESP32 (أو 5V لو المستشعر يدعم ومصدر الطاقة قوي).
    GND: وصل بأي GND على الـ ESP32.
    OUT: وصل بـ Pin Analog (مثلاً Pin 34، وهو ADC1_6 على الـ ESP32).
    ملاحظة: الـ ESP32 بيدعم قراءة Analog على Pins معينة (ADC1: Pins 32-39). تجنب استخدام Pins زي 0 أو 2 لو ممكن لأنها بتُستخدم لأغراض تانية أحيانًا.

الكود لاختبار مستشعر URM09 واحد:

مش هنحتاج مكتبة NewPing لأن الـ URM09 بيطلع إشارة Analog. هنستخدم دالة analogRead() المدمجة في Arduino لقراءة الجهد وتحويله لمسافة.
cpp
#define ANALOG_PIN 34 // Pin لقراءة الإشارة التناظرية من URM09
#define VCC_VOLTAGE 3.3 // جهد التشغيل (3.3V أو 5V حسب التوصيل)

void setup() {
  Serial.begin(115200);
  Serial.println("Starting URM09 Ultrasonic Test...");
}

void loop() {
  int analogValue = analogRead(ANALOG_PIN); // اقرأ القيمة التناظرية
  float voltage = (analogValue * VCC_VOLTAGE) / 4095.0; // تحويل القراءة إلى جهد (ESP32 ADC 12-bit)
  float distance = (voltage * 500.0) / VCC_VOLTAGE; // تحويل الجهد إلى مسافة (2-500 سم)

  Serial.print("Analog Value: ");
  Serial.print(analogValue);
  Serial.print(" | Voltage: ");
  Serial.print(voltage, 2);
  Serial.print(" V | Distance: ");
  Serial.print(distance, 1);
  Serial.println(" cm");

  delay(100); // انتظر 100 مللي ثانية بين القراءات
}
شرح الكود:

    analogRead(ANALOG_PIN): بيقرأ القيمة التناظرية من Pin 34 (من 0 إلى 4095 لأن الـ ESP32 بيستخدم 12-bit ADC).
    voltage: تحول القراءة إلى جهد بناءً على VCC (3.3V في المثال).
    distance: تحول الجهد إلى مسافة بناءً على مواصفات الـ URM09 (0V = 2 سم، 3.3V = 500 سم).
    الكود بيطبع القراءة الخام، الجهد، والمسافة على الـ Serial Monitor.

التشغيل:

    وصل الـ URM09 زي ما وصفنا (VCC → 3.3V, GND → GND, OUT → Pin 34).
    ارفع الكود على الـ ESP32 باستخدام Arduino IDE.
    افتح الـ Serial Monitor (ضبط السرعة على 115200) وشوف القراءات.
        لو شغال، هتشوف شيء زي: Analog Value: 1234 | Voltage: 1.00 V | Distance: 150.0 cm.
        لو المسافة مش منطقية:
            تأكد إنك موصل VCC على 3.3V (أو 5V لو استخدمته، وعدل VCC_VOLTAGE في الكود إلى 5.0).
            حرك جسم قدام المستشعر (من 2 سم إلى 500 سم) وشوف لو القراءات بتتغير.
            لو القراءات ثابتة أو 0، تفقد الاتصالات أو جرب Pin Analog تاني (زي Pin 35).

لو عايز تختبر الأربعة مستشعرات URM09:

لو الاختبار الأول نجح، نقدر نختبر الأربعة مع بعض باستخدام Pins Analog مختلفة. مثال:

    مستشعر 1: OUT → Pin 34
    مستشعر 2: OUT → Pin 35
    مستشعر 3: OUT → Pin 32
    مستشعر 4: OUT → Pin 33

الكود:
cpp
#define ANALOG_PIN_1 34 // مستشعر 1
#define ANALOG_PIN_2 35 // مستشعر 2
#define ANALOG_PIN_3 32 // مستشعر 3
#define ANALOG_PIN_4 33 // مستشعر 4
#define VCC_VOLTAGE 3.3 // جهد التشغيل

void setup() {
  Serial.begin(115200);
  Serial.println("Starting 4 URM09 Ultrasonic Sensors Test...");
}

void loop() {
  // قراءة المستشعرات
  int analogValue1 = analogRead(ANALOG_PIN_1);
  int analogValue2 = analogRead(ANALOG_PIN_2);
  int analogValue3 = analogRead(ANALOG_PIN_3);
  int analogValue4 = analogRead(ANALOG_PIN_4);

  // تحويل القراءات إلى مسافات
  float distance1 = ((analogValue1 * VCC_VOLTAGE) / 4095.0) * (500.0 / VCC_VOLTAGE);
  float distance2 = ((analogValue2 * VCC_VOLTAGE) / 4095.0) * (500.0 / VCC_VOLTAGE);
  float distance3 = ((analogValue3 * VCC_VOLTAGE) / 4095.0) * (500.0 / VCC_VOLTAGE);
  float distance4 = ((analogValue4 * VCC_VOLTAGE) / 4095.0) * (500.0 / VCC_VOLTAGE);

  // طباعة القراءات
  Serial.print("Sonar 1: "); Serial.print(distance1, 1); Serial.print(" cm | ");
  Serial.print("Sonar 2: "); Serial.print(distance2, 1); Serial.print(" cm | ");
  Serial.print("Sonar 3: "); Serial.print(distance3, 1); Serial.print(" cm | ");
  Serial.print("Sonar 4: "); Serial.print(distance4, 1); Serial.println(" cm");

  delay(100);
}
الخطوة الثانية: الخطوات الجاية

لو الـ URM09 اشتغل كويس، نقدر ننتقل لاختبار مكون تاني. اقتراحات:

    Servo Motor: اختبار حركة السيرفو بزوايا مختلفة (سهل وسريع).
    ESC: اختبار التحكم في الموتور (محتاج مصدر طاقة خارجي).
    دمج الـ IMU مع الـ Ultrasonic: لو عايز تجمع بيانات الـ BNO055 مع الـ URM09 في كود واحد على الـ ESP32.
    ربط الـ ESP32 بالـ Jetson Nano: لو عايز تبدأ ترسل بيانات المستشعرات (IMU + Ultrasonic) للـ Nano.

أسئلة ليك:

    الـ URM09 شغال كويس مع الكود الأول؟ لو فيه مشكلة، قولي إيه اللي بيظهر على الـ Serial Monitor.
    عايز ننتقل لأي مكون بعد الـ Ultrasonic؟ (Servo، ESC، أو دمج مع الـ Jetson Nano؟)
    لو هتستخدم مصدر طاقة خارجي (زي بطارية للـ URM09 أو الـ ESC)، قولي مواصفاته عشان أساعدك في التوصيلات.
    عايز ندمج الـ Ultrasonic مع الـ IMU في كود واحد دلوقتي، ولا نكمل اختبار باقي المكونات الأول؟

قولي إيه الخطوة الجاية اللي عايز تركز عليها، وهنمشي معاك بالتفصيل! 😄
2.8s
ممكن تشرحلي التوصيلات

تمام، بما إنك عايز تختبر مستشعر Gravity: URM09 Ultrasonic Distance Sensor (Analog) على الـ ESP32، وكمان عندك مكونات زي BNO055، BMP280، 4 مستشعرات URM09، Servo Motor، ESC، و Jetson Nano، هنركز دلوقتي على توصيلات الـ URM09 بشكل مفصل عشان تتأكد إن كل حاجة شغالة صح. وبعدين لو عايز ننتقل لتوصيلات مكون تاني (زي الـ Servo أو الـ ESC)، قولي وهنكمل خطوة بخطوة. 😊
توصيلات مستشعر Gravity: URM09 Ultrasonic (Analog) على ESP32
مواصفات الـ URM09:

    الأطراف:
        VCC: مصدر الطاقة (يدعم 3.3V أو 5V).
        GND: الأرضي.
        OUT: إشارة تناظرية (Analog Output) بتطلع جهد يعتمد على المسافة (من 0V إلى VCC).
    نطاق القياس: 2 سم إلى 500 سم.
    الإخراج: جهد تناظري يتحول لمسافة (مثلاً، عند 3.3V: 0V = 2 سم، 3.3V = 500 سم).
    التوافق مع ESP32: الـ ESP32 بيدعم قراءة الإشارة التناظرية عبر قنوات ADC1 (مثل Pins 32-39).

التوصيلات لمستشعر URM09 واحد:

    VCC (في الـ URM09):
        وصل بـ 3.3V Pin على الـ ESP32 (يفضل 3.3V عشان تتجنب مشاكل الجهد مع الـ ESP32).
        لو عايز تستخدم 5V (لو مصدر الطاقة قوي)، وصل بـ 5V Pin (VIN) على الـ ESP32، بس هتحتاج مصدر طاقة خارجي زي محول USB أو بطارية.
    GND (في الـ URM09):
        وصل بأي GND Pin على الـ ESP32 (فيه أكتر من GND Pin، زي اللي جنب 3.3V أو 5V).
    OUT (في الـ URM09):
        وصل بـ Pin 34 (أو أي Pin Analog آخر من ADC1: 32، 33، 35، 36، 39).
        ملاحظة: تجنب Pins زي 0، 2، أو 15 لأنها ممكن تُستخدم لأغراض تانية (زي البوت أو Wi-Fi).

مثال تخطيط التوصيلات:
text
URM09         ESP32
-----         -----
VCC   ----->  3.3V
GND   ----->  GND
OUT   ----->  Pin 34
نصايح للتوصيل:

    مصدر الطاقة: لو بتستخدم USB لتشغيل الـ ESP32، الـ 3.3V Pin بيكون كافي لتشغيل مستشعر URM09 واحد. لو هتوصل الأربعة مستشعرات، ممكن تحتاج مصدر طاقة خارجي (زي بطارية 5V/2A) عشان تتأكد إن التيار كافي.
    الكابلات: استخدم أسلاك قصيرة ومتينة (مثل Jumper Wires) عشان تقلل الضوضاء في الإشارة التناظرية.
    تثبيت المستشعر: ثبت الـ URM09 في مكان ثابت (زي قاعدة الروبوت) وتأكد إن واجهته موجهة للأمام بدون عوائق قريبة (أقل من 2 سم).

اختبار المستشعر الأول:

استخدم الكود ده (من الرد السابق) لاختبار المستشعر الأول:
cpp
#define ANALOG_PIN 34 // Pin لقراءة الإشارة التناظرية
#define VCC_VOLTAGE 3.3 // جهد التشغيل (3.3V أو 5V حسب التوصيل)

void setup() {
  Serial.begin(115200);
  Serial.println("Starting URM09 Ultrasonic Test...");
}

void loop() {
  int analogValue = analogRead(ANALOG_PIN);
  float voltage = (analogValue * VCC_VOLTAGE) / 4095.0;
  float distance = (voltage * 500.0) / VCC_VOLTAGE;

  Serial.print("Analog Value: ");
  Serial.print(analogValue);
  Serial.print(" | Voltage: ");
  Serial.print(voltage, 2);
  Serial.print(" V | Distance: ");
  Serial.print(distance, 1);
  Serial.println(" cm");

  delay(100);
}

    التشغيل: ارفع الكود على الـ ESP32، وافتح الـ Serial Monitor (115200 baud). لو التوصيلات صح، هتشوف قراءات المسافة (مثلاً Distance: 150.0 cm) بناءً على العوائق قدام المستشعر.
    لو فيه مشكلة:
        لو القراءات 0 أو مش منطقية، تفقد توصيلات VCC، GND، و OUT.
        تأكد إنك موصل VCC على 3.3V (أو 5V وعدل VCC_VOLTAGE في الكود).
        جرب Pin Analog تاني (مثل 35) لو Pin 34 ما اشتغلش.

توصيلات الأربعة مستشعرات URM09:

لو الاختبار الأول نجح، نقدر نوصل الأربعة مستشعرات معًا. كل مستشعر هيتصل بنفس الطريقة، بس هنستخدم Pins Analog مختلفة.
التوصيلات:
text
URM09 #1:
  VCC   ----->  3.3V
  GND   ----->  GND
  OUT   ----->  Pin 34

URM09 #2:
  VCC   ----->  3.3V
  GND   ----->  GND
  OUT   ----->  Pin 35

URM09 #3:
  VCC   ----->  3.3V
  GND   ----->  GND
  OUT   ----->  Pin 32

URM09 #4:
  VCC   ----->  3.3V
  GND   ----->  GND
  OUT   ----->  Pin 33
ملاحظات:

    توزيع الطاقة: لو بتوصل الأربعة مستشعرات على 3.3V من الـ ESP32، ممكن التيار ما يكفيش لو USB بس. جرب استخدم Power Distribution Board أو مصدر طاقة خارجي (زي بطارية 3.3V/2A أو 5V/2A).
    اختيار Pins: الـ ESP32 بيدعم قنوات ADC1 (Pins 32-39) للقراءة التناظرية. تأكد إنك بتستخدم Pins من القنوات دي فقط.
    تجنب الضوضاء: لو لاحظت قراءات غير مستقرة، استخدم مكثف (Capacitor) 10µF بين VCC و GND لكل مستشعر عشان تستقر الطاقة.

الكود للأربعة مستشعرات:
cpp
#define ANALOG_PIN_1 34 // مستشعر 1
#define ANALOG_PIN_2 35 // مستشعر 2
#define ANALOG_PIN_3 32 // مستشعر 3
#define ANALOG_PIN_4 33 // مستشعر 4
#define VCC_VOLTAGE 3.3 // جهد التشغيل

void setup() {
  Serial.begin(115200);
  Serial.println("Starting 4 URM09 Ultrasonic Sensors Test...");
}

void loop() {
  int analogValue1 = analogRead(ANALOG_PIN_1);
  int analogValue2 = analogRead(ANALOG_PIN_2);
  int analogValue3 = analogRead(ANALOG_PIN_3);
  int analogValue4 = analogRead(ANALOG_PIN_4);

  float distance1 = ((analogValue1 * VCC_VOLTAGE) / 4095.0) * (500.0 / VCC_VOLTAGE);
  float distance2 = ((analogValue2 * VCC_VOLTAGE) / 4095.0) * (500.0 / VCC_VOLTAGE);
  float distance3 = ((analogValue3 * VCC_VOLTAGE) / 4095.0) * (500.0 / VCC_VOLTAGE);
  float distance4 = ((analogValue4 * VCC_VOLTAGE) / 4095.0) * (500.0 / VCC_VOLTAGE);

  Serial.print("Sonar 1: "); Serial.print(distance1, 1); Serial.print(" cm | ");
  Serial.print("Sonar 2: "); Serial.print(distance2, 1); Serial.print(" cm | ");
  Serial.print("Sonar 3: "); Serial.print(distance3, 1); Serial.print(" cm | ");
  Serial.print("Sonar 4: "); Serial.print(distance4, 1); Serial.println(" cm");

  delay(100);
}
التشغيل:

    ارفع الكود وشوف الـ Serial Monitor. لو كل حاجة صح، هتشوف قراءات الأربعة مستشعرات (مثلاً: Sonar 1: 120.5 cm | Sonar 2: 200.0 cm | ...).
    لو مستشعر معين مش بيشتغل، تفقد توصيلاته (خاصة OUT Pin) أو جرب استبداله بمستشعر تاني.

إذا كنت عايز دمج مع مكونات تانية:

لو الـ URM09 اشتغل كويس، ممكن ندمجه مع الـ BNO055 و BMP280 (اللي اختبرتهم قبل كده). مثال بسيط لكود مدمج:
cpp
#include <Wire.h>
#include "DFRobot_BNO055.h"
#include <Adafruit_BMP280.h>

#define ANALOG_PIN_1 34 // URM09 مستشعر 1
#define VCC_VOLTAGE 3.3
#define I2C_SDA 21
#define I2C_SCL 22

DFRobot_BNO055_IIC bno(&Wire, 0x28);
Adafruit_BMP280 bmp;

void printLastOperateStatus(DFRobot_BNO055_IIC::eStatus_t eStatus) {
  switch (eStatus) {
    case DFRobot_BNO055_IIC::eStatusOK: Serial.println("Everything OK"); break;
    case DFRobot_BNO055_IIC::eStatusErr: Serial.println("Unknown error"); break;
    case DFRobot_BNO055_IIC::eStatusErrDeviceNotDetect: Serial.println("Device not detected"); break;
    case DFRobot_BNO055_IIC::eStatusErrDeviceReadyTimeOut: Serial.println("Device ready timeout"); break;
    case DFRobot_BNO055_IIC::eStatusErrDeviceStatus: Serial.println("Device internal status error"); break;
    default: Serial.println("Unknown status"); break;
  }
}

void setup() {
  Serial.begin(115200);
  Serial.println("Starting Sensors...");

  Wire.begin();
  bno.reset();
  delay(1000);
  while (bno.begin() != DFRobot_BNO055_IIC::eStatusOK) {
    Serial.println("BNO055 initialization failed");
    printLastOperateStatus(bno.lastOperateStatus);
    delay(2000);
  }
  Serial.println("BNO055 initialized successfully");

  if (!bmp.begin(0x76)) {
    Serial.println("BMP280 initialization failed");
    while (1) delay(2000);
  }
  Serial.println("BMP280 initialized successfully");
}

void loop() {
  // BNO055 Data
  DFRobot_BNO055_IIC::sEulAnalog_t sEul = bno.getEul();
  Serial.print("Pitch: "); Serial.print(sEul.pitch, 3);
  Serial.print(" | Roll: "); Serial.print(sEul.roll, 3);
  Serial.print(" | Yaw: "); Serial.print(sEul.head, 3);

  // BMP280 Data
  Serial.print(" | Pressure: "); Serial.print(bmp.readPressure() / 100.0F, 2);
  Serial.print(" hPa");

  // URM09 Data
  int analogValue1 = analogRead(ANALOG_PIN_1);
  float distance1 = ((analogValue1 * VCC_VOLTAGE) / 4095.0) * (500.0 / VCC_VOLTAGE);
  Serial.print(" | Sonar 1: "); Serial.print(distance1, 1); Serial.println(" cm");

  delay(100);
}
الخطوة الجاية: اختبار مكونات تانية

لو الـ URM09 اشتغل كويس، نقدر ننتقل لمكون تاني. التوصيلات لباقي المكونات:

    Servo Motor:
        VCC: وصل بـ 5V (مصدر خارجي لأن السيرفو بيسحب تيار كبير).
        GND: وصل بـ GND (مشترك مع الـ ESP32).
        Signal: وصل بـ PWM Pin (مثل Pin 13).
    ESC:
        VCC/GND: وصل بمصدر طاقة خارجي (زي بطارية LiPo 7.4V أو 11.1V حسب الـ ESC).
        Signal: وصل بـ PWM Pin (مثل Pin 12).
        ملاحظة: الـ ESC محتاج معايرة (Calibration) قبل الاستخدام.
    Jetson Nano مع ESP32:
        ممكن نوصل الـ ESP32 بالـ Nano عبر Serial (TX/RX Pins) أو Wi-Fi (باستخدام MQTT أو UDP) لنقل بيانات المستشعرات.

أسئلة ليك:

    الـ URM09 شغال كويس مع التوصيلات والكود؟ لو فيه مشكلة، قولي إيه اللي بيظهر على الـ Serial Monitor.
    عايز نختبر مكون معين بعد الـ URM09؟ (Servo، ESC، أو دمج مع الـ Jetson Nano؟)
    لو عايز توصيلات مكون تاني (زي الـ Servo أو الـ ESC)، قولي نوعه بالضبط (مثلاً نوع الـ Servo أو الـ ESC) عشان أعطيك التوصيلات المناسبة.
    عندك مصدر طاقة خارجي (زي بطارية) للـ URM09 أو الـ ESC؟ لو أيوه، قولي مواصفاته (فولت وأمبير).
